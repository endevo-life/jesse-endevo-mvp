{
  "version": 2,
  "builds": [
    {
      "src": "server.ts",
      "use": "@vercel/node",
      "config": {
        "includeFiles": ["logo_resized.png", "Jesse-image.png"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "server.ts" }
  ]
}
