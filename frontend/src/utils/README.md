# Utils

Helper functions and constants.

## Examples:
- `constants.ts` - Questions data, domain weights, tier thresholds
- `scoring.ts` - Client-side score calculation helpers
- `formatters.ts` - Date, number, text formatters
